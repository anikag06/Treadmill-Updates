import { LoadingBarDirective } from './loading-bar.directive';

describe('LoadingBarDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingBarDirective();
    expect(directive).toBeTruthy();
  });
});

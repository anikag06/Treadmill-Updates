import { LoadingBarChildDirective } from './loading-bar-child.directive';

describe('LoadingBarChildDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadingBarChildDirective();
    expect(directive).toBeTruthy();
  });
});

import { ChatImageDirective } from './chat-image.directive';

describe('ChatImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatImageDirective();
    expect(directive).toBeTruthy();
  });
});

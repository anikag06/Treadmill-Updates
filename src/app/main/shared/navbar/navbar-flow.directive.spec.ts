import { NavbarFlowDirective } from './navbar-flow.directive';

describe('NavbarFlowDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarFlowDirective();
    expect(directive).toBeTruthy();
  });
});

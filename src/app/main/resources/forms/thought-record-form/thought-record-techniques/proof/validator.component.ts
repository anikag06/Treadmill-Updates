// import {FormArray, FormGroup} from '@angular/forms';
//
// customValidationFunction = (formGroup: FormGroup): any => {
//     const arrayForControl = formGroup.get(
//         'favorEvidences',
//     ) as FormArray;
//     const favorField = arrayForControl.at(0).value.evidence;
//
//     const arrayAgainstControl = formGroup.get(
//         'againstEvidences',
//     ) as FormArray;
//     const againstField = arrayAgainstControl.at(0).value.evidence;
//     return favorField.length > 1 && againstField.length > 1;
// };

export class VideoItem {
  constructor(
    public id: number,

    public title: string,
    public url: string,
    public preview_img: string,
  ) {
    // this.id = id;
    // this.title = title;
    // this.url = url;
  }
}
